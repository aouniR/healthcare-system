spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aouniR/healthcare-system-config.git
          searchPaths: '{application}'
server:
  port: 8888
